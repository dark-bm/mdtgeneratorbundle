    /**
     * Get {{ entity }} entity filters.
     *
     * @ApiDoc(
     *   resource = true,
     *   section = "{{ bundle }}",
     *   description = "Get {{ entity }} entity filters",
     *   statusCodes = {
     *     200 = "Returned when successful",
     *     204 = "Returned when no results",
     *     500 = "Returned when error"
     *   }
     * )
     *
     * @View()
     *
     * @return Response
     *
     */
    public function optionsAction()
    {
        $options = [
            'filters'=>[
                'names'=>[
    {% for field in properties %}
                    '{{ field }}'=>'Filter name for {{ field }}',
    {% endfor %}
                ],
                'values'=>[
    {% for field in properties %}
                    '{{ field }}'=>[],
    {% endfor %}
                ],
            ],
            'associations' => [
    {% for field in associatons %}
                '{{ field }}'=>'NO_LINK',
    {% endfor %}
            ],
        ];
        return $options;
    }
