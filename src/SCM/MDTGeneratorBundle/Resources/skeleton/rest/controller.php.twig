<?php

namespace {{ namespace }}\Controller{{ entity_namespace ? '\\' ~ entity_namespace : '' }};

use {{ namespace }}\Entity\{{ entity }};
use {{ namespace }}\Form\{{ entity }}Type;

use FOS\RestBundle\Controller\FOSRestController;
use Sensio\Bundle\FrameworkExtraBundle\Configuration\ParamConverter;
use Sensio\Bundle\FrameworkExtraBundle\Configuration\Route;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use FOS\RestBundle\Controller\Annotations as Rest;
use Nelmio\ApiDocBundle\Annotation\ApiDoc;


 /**
 * {{ entity }} controller.
 *
 * @Route("/{{ route_prefix }}", options={"expose"=true})
 */
class {{ entity_class }}Controller extends FOSRestController
{
    {%- include 'rest/actions/listAction.php.twig' %}

    {%- include 'rest/actions/createAction.php.twig' %}

    {%- include 'rest/actions/putAction.php.twig' %}

    {%- include 'rest/actions/viewAction.php.twig' %}

    {%- include 'rest/actions/deleteAction.php.twig' %}

}
